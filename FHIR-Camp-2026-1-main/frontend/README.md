## Frontend (React) — ASMED FHIR Lab

### Requisitos
- Node.js 18+

### Instalar

```bash
npm install
```

### Ejecutar

```bash
npm run dev
```

El frontend hace proxy a `http://localhost:8000` para rutas `/api`.
